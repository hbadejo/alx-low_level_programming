#include "main.h"

/**
 * mul - return the multiplication of 2 integers
 * @_: First int
 * @_i: Second int
 * Return: the result ofthe multiplication
 */

int mul(int _, int _i)
{
	int mult;

	mult = (_ * _i);
	return (mult);
}
